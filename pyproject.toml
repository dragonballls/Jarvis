[project]
name = "standalone-self-coding-engine"
version = "1.0.0"
description = "Framework-independent bounded self-coding engine"
requires-python = ">=3.10"
dependencies = []

[build-system]
requires = ["setuptools>=69"]
build-backend = "setuptools.build_meta"

[tool.pytest.ini_options]
pythonpath = ["."]
