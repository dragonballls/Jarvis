name: Jarvis integration verification

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

concurrency:
  group: jarvis-integration-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  mark53:
    name: Mark 53 boundary
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install Mark 53 test dependencies
        run: python -m pip install --disable-pip-version-check pytest httpx
      - name: Verify Mark 53 component
        run: |
          set -euo pipefail
          python -m compileall -q mark53
          python -m pytest -q tests/test_component_boundaries.py -k 'mark53'

  mark-updater:
    name: Mark updater
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install Mark updater test dependencies
        run: python -m pip install --disable-pip-version-check pytest
      - name: Verify Mark updater boundary
        run: |
          set -euo pipefail
          python -m compileall -q mark_updater
          python -m pytest -q tests/test_component_boundaries.py -k 'mark_updater'

  self-coding:
    name: Jarvis self-coding
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install self-coding dependencies
        run: python -m pip install --disable-pip-version-check -r requirements.txt
      - name: Verify self-coding boundary
        run: |
          set -euo pipefail
          python -m compileall -q agent self_coding
          python -m pytest -q tests/test_component_boundaries.py -k 'self_coding'
          python -m pytest -q tests/test_self_coding_git_boundary.py
          python -m pytest -q \
            tests/test_agent_coding_routing.py \
            tests/test_agent_coding_rollback_boundary.py \
            tests/test_agent_coding_session_integration.py \
            tests/test_agent_safe_coding_integration.py

  openhands:
    name: OpenHands
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install OpenHands
        run: python -m pip install --disable-pip-version-check -r requirements.txt
      - name: Verify OpenHands boundary
        run: |
          set -euo pipefail
          python -c "import openhands.sdk, openhands.tools; print('OpenHands import OK')"
          python -m pytest -q tests/test_agent_integrations.py -k 'openhands'

  emrg:
    name: EMRG
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install EMRG
        run: python -m pip install --disable-pip-version-check -r requirements.txt
      - name: Verify EMRG boundary
        run: |
          set -euo pipefail
          python -c "import emrg; print('EMRG import OK')"
          python -m pytest -q tests/test_agent_integrations.py -k 'emrg or evolution'

  gods-eye:
    name: God's Eye
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r requirements.txt
      - name: Verify God's Eye
        run: |
          set -euo pipefail
          python -m compileall -q core integrations plugins
          python -m pytest -q \
            core/test_gods_eye_bridge.py \
            core/test_gods_eye_context.py \
            core/gods_eye_intent_examples_test.py \
            integrations/gods_eye/test_adapter.py \
            integrations/gods_eye/test_contract.py \
            tests/test_gods_eye_intent.py \
            tests/test_gods_eye_intent_safety.py \
            tests/test_gods_eye_plugin_discovery.py

  providers:
    name: Providers and routing
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install dependencies
        run: python -m pip install --disable-pip-version-check -r requirements.txt
      - name: Verify providers
        run: |
          set -euo pipefail
          python -m compileall -q providers config core
          python -m pytest -q tests/test_provider_routing.py tests/test_provider_fallback.py tests/test_blackout.py

  jarvis-updater:
    name: Jarvis updater
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Verify updater syntax and contract
        run: |
          set -euo pipefail
          python -m py_compile scripts/update.py packaging/windows/app.py packaging/windows/build.py
          grep -q 'JARVIS_AUTO_UPDATE_EXE' packaging/windows/app.py
          grep -q 'JARVIS_UPDATED_RESTART' packaging/windows/app.py
          grep -q 'Jarvis.exe' packaging/windows/build.py

  boundaries:
    name: Component boundary gate
    runs-on: ubuntu-latest
    timeout-minutes: 5
    needs: [mark53, mark-updater, self-coding, openhands, emrg, gods-eye, providers, jarvis-updater]
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v7
        with:
          python-version: "3.12"
      - name: Install boundary test dependencies
        run: python -m pip install --disable-pip-version-check pytest
      - name: Verify isolated namespaces
        run: |
          set -euo pipefail
          python -m compileall -q mark53 mark_updater self_coding
          python -m pytest -q tests/test_component_boundaries.py
